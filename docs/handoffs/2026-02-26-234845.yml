meta:
  date: 2026-02-26
  author: claude
  phase: "Fas 1: Grund"
  status: aktiv
  session_notes: |
    Session 5. Planerade grafisk profil + omstrukturerade roadmap och konventioner.

    Gjort:
    - Utforskade hela kodbasen: hittade ~50+ emoji-forekomster, 11 SVG-ikoner + 4 illustrationer som ALDRIG anvands
    - Skrev 9-stegs implementationsplan for grafisk profil (docs/plans/1-grafisk-profil.yml)
    - Omstrukturerade ROADMAP.md: faser som teman (inga kvartal), backlog, ideer-sektion
    - Ny namnkonvention for planfiler: {fas}-{slug}.yml
    - Lade till emoji-forbudsregel i CLAUDE.md
    - Lade till planfil-konvention i CLAUDE.md
    - Dopte om planfiler till ny konvention
    - Sparade anvandarpref i memory: inga emojis, professionellt och rent

tech_stack:
  current:
    - Vite 7.3 (build)
    - React 19 (hooks, JSX)
    - TypeScript (strict mode)
    - Recharts 3.7 (NutritionChart)
    - "@fontsource/fraunces + @fontsource/lora (self-hosted)"
    - CSS Modules + CSS custom properties
  planned:
    app: "React Native + Expo (framtida, betald version)"

structure:
  src:
    - src/components/icons/index.tsx (11 SVG-ikoner, OANVANDA)
    - src/components/illustrations/ (4 illustrationer, OANVANDA)
    - src/theme/tokens.ts (komplett designsystem)
    - src/data/types.ts (typdefinitioner)
    - src/data/crops/*.ts (5 grodfiler med emoji)
    - src/components/pages/ (CropList, CropPage, ZoneSelector)
    - src/components/crop/ (18 sektionskomponenter)
    - src/components/ui/ (Section, DifficultyBadge, ZoneBadge m.fl.)
  docs:
    - docs/plans/1-grafisk-profil.yml (9-stegs plan, redo att implementera)
    - docs/plans/1-separera-data.yml (klar)
    - docs/ROADMAP.md (omstrukturerad: faser + backlog + ideer)

database: null
api_endpoints: null

commands:
  - "npm run dev    # Starta Vite dev server"
  - "npm run build  # Bygger till dist/"
  - "npx tsc --noEmit  # Typkontroll utan output"

next_steps:
  - "Implementera grafisk profil-planen (docs/plans/1-grafisk-profil.yml)"
  - "  Steg 1: Skapa Icon-registry (Icon.tsx) + CropGraphic.tsx"
  - "  Steg 2: Skapa ~20 nya SVG-ikoner"
  - "  Steg 3: Skapa PotatoIllustration"
  - "  Steg 4-5: Uppdatera types.ts + alla datafiler (emoji -> semantiska ID)"
  - "  Steg 6-8: Uppdatera Section + alla komponenter"
  - "  Steg 9: Slutverifiering"

context:
  decisions:
    - "INGA EMOJIS i appen -- all grafik ska vara egengjord custom SVG"
    - "Roadmap omstrukturerad: faser = teman (inga kvartal), backlog for konkreta steg, ideer for lost tankegods"
    - "Planfil-konvention: {fas}-{slug}.yml"
    - "Icon-registry pattern: data lagrar semantiska string-ID, en central Icon-komponent mappar till SVG"
    - "CropGraphic-komponent: mappar crop-ID till illustration/ikon med fallback"
    - "Grafisk profil prioriteras fore korsreferenser"
  constraints:
    - "Aldrig unicode-emojis i data eller UI"
    - "Alla ikoner: 1.8px stroke, round caps, 24x24, monokrom, subtil fill (0.12-0.20 opacity)"
    - "All text pa svenska, kompis-ton"

known_issues:
  - "11 SVG-ikoner + 4 illustrationer finns men anvands inte i appen (fixas av grafisk profil)"
  - "Potatis saknar illustration"
  - "Bundle size >500KB pga Recharts"
  - "Inga korsreferenser annu"
