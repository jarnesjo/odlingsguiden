meta:
  date: 2026-02-27
  author: claude
  phase: "Fas 1: Grund"
  status: avslutad
  session_notes: |
    Session 7. Fas 1.3 Grafisk profil — KLAR.

    Gjort:
    - 24 nya SVG-ikoner i icons/index.tsx (Search, Temperature, Wind, Indoor,
      Hardening, Transplant, Harvest, Tip, Warning, Snowflake, Cellar, Bloom,
      Hill, Stake, Leaf, Rotation, Bug, Tag, Chart, Storage, Flower, BackArrow,
      Check, Seedling + DifficultyDot)
    - Icon.tsx — central registry som mappar semantiska string-ID till SVG-komponenter
    - CropGraphic.tsx — mappar grod-ID till illustration/ikon med kategori-fallback
    - PotatoIllustration.tsx — 200x200 profilbild + 48x48 listikon
    - types.ts: emoji → icon i CategoryInfo, CropListEntry, Crop, Companion
    - Alla 5 grodfiler + categories.ts + difficulty.ts: 50+ emoji-strangar → semantiska ID
    - Section.tsx: ny icon-prop for SVG fore titel
    - CropList: LogoIcon, kategori-ikoner, CropGraphic, SearchIcon, DifficultyDot
    - CropHeader: CropGraphic illustration + BackArrowIcon
    - Alla 18 sektionskomponenter + DifficultyBadge: SVG i titlar och inline-innehall
    - Noll emoji kvar i src/ — allt ar custom SVG
    - 5 commits, ej pushade

    Anvandaren namnde tva saker:
    1. Dark mode — redan i Ideer-sektionen
    2. Odlingszonerna behover faktagranskas (Gotland = zon 1 etc) — tillagd som kand brist

tech_stack:
  current:
    - Vite 7.3 (build)
    - React 19 (hooks, JSX)
    - TypeScript 5.9 (strict mode)
    - Recharts 3.7 (NutritionChart)
    - "@fontsource/fraunces + @fontsource/lora (self-hosted)"
    - CSS Modules + CSS custom properties
  planned:
    app: "React Native + Expo (framtida, betald version)"

structure:
  docs:
    - docs/ROADMAP.md
    - docs/guides/ny-groda.md
    - docs/guides/designsystem.md
    - docs/guides/svg-illustrationer.md
    - docs/plans/1.2-separera-data.yml (klar)
    - docs/plans/1.3-grafisk-profil.yml (KLAR)
    - docs/handoffs/
  src:
    - src/components/icons/index.tsx — 35 SVG-ikoner + DifficultyDot
    - src/components/icons/Icon.tsx — central registry (NY)
    - src/components/illustrations/ — 5 illustrationer + CropGraphic.tsx (NY)
    - src/components/brand/ — 4 symboler, AppIcon, Favicon, LogoCombined
    - src/theme/tokens.ts
    - src/data/types.ts (emoji → icon)
    - src/data/crops/*.ts (5 grodfiler, semantic icon IDs)
    - src/components/pages/ (CropList, CropPage, ZoneSelector)
    - src/components/crop/ (18 sektionskomponenter)
    - src/components/ui/ (Section med icon-prop, DifficultyBadge med DifficultyDot)
  public:
    - public/brand/ — 7 statiska SVG-filer

database: null
api_endpoints: null

commands:
  - "npm run dev    # Starta Vite dev server"
  - "npm run build  # Bygger till dist/"
  - "npx tsc --noEmit  # Typkontroll utan output"

next_steps:
  - "Pusha 5 commits till GitHub (git push)"
  - "Visuell QA — starta dev server och granska alla vyer"
  - "Nasta roadmap-steg: 1.4 Korsreferenser eller 1.5 Sasongsvy"
  - "Faktagranska odlingszonerna (ZONE_INFO i src/data/zones.ts)"

context:
  decisions:
    - "Companion.emoji → icon med generiska kategori-ID (vegetable/herb/berry/flower)"
    - "DifficultyInfo.icon → semantisk string + DifficultyDot-komponent med fargad cirkel"
    - "CropGraphic fallback: kategori-ikon for groder utan illustration"
    - "Section-titlar: ikon som ReactNode-prop, inte del av title-strangen"
  constraints:
    - "Aldrig unicode-emojis i data eller UI"
    - "Alla ikoner: 1.8px stroke, round caps, 24x24, monokrom, subtil fill"
    - "All text pa svenska, kompis-ton"

known_issues:
  - "Odlingszonerna behover faktagranskas — Gotland ar zon 1 men stammer det i var data?"
  - "5 commits ej pushade till GitHub"
  - "Bundle size >500KB pga Recharts"
  - "prototypes/ kan arkiveras/tas bort — allt ar migrerat"
